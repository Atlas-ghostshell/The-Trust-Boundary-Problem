# The-Trust-Boundary-Problem
Three CVSS 7.5 findings in a commercial captive WiFi portal — a payment bypass (fixed), unlawful surveillance (unresolved), and a plaintext credential leak (partial) — all tracing back to the same root cause: trusting an identifier the server never actually controlled.
